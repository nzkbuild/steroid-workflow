# Install System

This directory is reserved for the modular installer flow:

- runtime installation
- IDE rule installation
- git hook installation

During the transition, `bin/cli.js` remains the live installer entrypoint.
